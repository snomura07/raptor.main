#include <string>
#include <RaptorBase/RaptorBase.h>
#include "../config/AppConfig.h"

class ****** : public RaptorBase{
public:
    ******();
    ~******();
    bool run();

private:
    AppConfig config;
    unsigned long count;
};
